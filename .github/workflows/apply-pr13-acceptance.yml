name: Apply PR13 acceptance follow-up

on:
  push:
    branches:
      - claude/binding-handoff-review-0ajqcl
    paths:
      - tools/apply_pr13_acceptance.py
      - .github/workflows/apply-pr13-acceptance.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: claude/binding-handoff-review-0ajqcl
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply acceptance changes
        run: python tools/apply_pr13_acceptance.py
      - name: Syntax checks
        run: |
          python -m compileall -q app tests
          git diff --check
      - name: Commit acceptance changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Complete PR13 acceptance semantics"
          git push origin HEAD:claude/binding-handoff-review-0ajqcl
